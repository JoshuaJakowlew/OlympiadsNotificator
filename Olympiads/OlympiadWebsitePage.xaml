<Page
    x:Class="Olympiads.OlympiadWebsitePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Olympiads"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="5*"/>
            <RowDefinition Height="13*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="23*"/>
            <ColumnDefinition Width="9*"/>
        </Grid.ColumnDefinitions>
        <WebView x:Name="OlympiadPageViewer" Grid.ColumnSpan="2" Grid.RowSpan="2" />
        <Grid Grid.Column="1" Background="#9FFFFFFF">
            <DatePicker HorizontalAlignment="Left" Margin="156,150,0,0" VerticalAlignment="Top"/>
            <Button Content="Button" HorizontalAlignment="Left" Margin="270,187,0,0" VerticalAlignment="Top"/>
        </Grid>
    </Grid>
</Page>
